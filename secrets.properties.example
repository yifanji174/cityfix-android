# Copy this file to secrets.properties in the same directory ( CityFix/ ).
# The real secrets.properties is gitignored — never commit it.
#
# After rotating any key that was ever in version control, put the new values here:
#
HUGGINGFACE_TOKEN=
GEMINI_API_KEY=
MAPS_API_KEY=
